import { AlwaysShowDirective } from './always-show-directive';

describe('AlwaysShowDirective', () => {
  it('should create an instance', () => {
    const directive = new AlwaysShowDirective();
    expect(directive).toBeTruthy();
  });
});
