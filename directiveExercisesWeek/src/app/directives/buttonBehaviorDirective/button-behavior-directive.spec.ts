import { ButtonBehaviorDirective } from './button-behavior-directive';

describe('ButtonBehaviorDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonBehaviorDirective();
    expect(directive).toBeTruthy();
  });
});
