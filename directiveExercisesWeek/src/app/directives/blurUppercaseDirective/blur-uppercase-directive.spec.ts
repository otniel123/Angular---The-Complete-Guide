import { BlurUppercaseDirective } from './blur-uppercase-directive';

describe('BlurUppercaseDirective', () => {
  it('should create an instance', () => {
    const directive = new BlurUppercaseDirective();
    expect(directive).toBeTruthy();
  });
});
