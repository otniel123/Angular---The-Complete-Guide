import { TemplateRefShowDirective } from './template-ref-show-directive';

describe('TemplateRefShowDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateRefShowDirective();
    expect(directive).toBeTruthy();
  });
});
