import { HoverLogDirective } from './hover-log-directive';

describe('HoverLogDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverLogDirective();
    expect(directive).toBeTruthy();
  });
});
